version: '3'
services:
  node-app:
    build:
      context: .
      args:
        - NODE_ENV=production
      # build: .
      # ports:
      #   - "3000:3000"
      # volumes:
      #   - ./:/app 
      #   - /app/node_modules
      # environment:
        # - NODE_ENV=production
    command: npm run prod
    # env_file:
    #   - ./.env

# we have simply commented out everything that is not needed.
